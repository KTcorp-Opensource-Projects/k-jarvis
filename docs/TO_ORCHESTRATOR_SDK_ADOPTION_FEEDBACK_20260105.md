# SDK 실제 적용 결과 및 전략 의견

**작성일**: 2026-01-05  
**From**: Agent Team  
**To**: Orchestrator Team, MCPHub (K-ARC) Team  
**상태**: ✅ **실제 테스트 기반 피드백**

---

## 🧪 실제 SDK 적용 테스트 결과

오케스트레이터 팀의 고민 문서를 받고, 직접 SDK를 만들어서 Sample Agent에 적용해 봤습니다.

### 테스트 개요

| 항목 | 내용 |
|------|------|
| SDK 이름 | `k-jarvis-sdk` |
| 적용 대상 | Sample AI Agent |
| 테스트 환경 | Docker Container |
| 결과 | ✅ 정상 동작 |

### 코드 감소 효과

| 파일 | 기존 | SDK 적용 후 | 감소율 |
|------|------|------------|--------|
| `a2a_server.py` | **364줄** | **138줄** | **62% 감소** |

### SDK 구성

```
k-jarvis-sdk/
├── k_jarvis/
│   ├── a2a/           # A2A 서버, 응답 빌더
│   ├── mcp/           # MCPHub 클라이언트 (Stateless HTTP)
│   ├── config/        # 표준 Settings 클래스
│   ├── errors/        # 표준 에러 코드
│   └── utils/         # 헤더 추출, 로깅
```

---

## 💡 오케스트레이터 팀 질문에 대한 답변

### 1. 현실성 판단

> **Q**: SDK만으로 외부 개발자가 우리 플랫폼에 쉽게 연동할 수 있다고 생각하시나요?

**A**: **SDK만으로는 부족합니다. 하지만 SDK는 필수입니다.**

우리가 직접 개발하면서 느낀 것:
- A2A 메서드명 불일치 (`SendMessage` vs `message/send`) → SDK로 자동 처리
- `mcp_hub_url` 필드 누락 → SDK BaseAgentSettings로 방지
- MCPHub Stateless 호환성 → SDK MCPClient로 해결

SDK가 없었다면 이런 문제들을 **모든 개발자가 직접 겪어야** 합니다.

### 2. 필요한 추가 요소

- [x] **상세 문서/튜토리얼** - 반드시 필요
- [x] **예제 프로젝트** - 우리 Agent들이 예제가 됨
- [x] **CLI 도구** - `k-jarvis create agent` 있으면 좋음
- [ ] Builder UI - 나중에
- [ ] Playground - 나중에
- [ ] 커뮤니티 - 나중에

### 3. 우선순위

```
A) SDK 먼저 완성 → 문서/예제 추가  ← 우리 의견
```

**이유**: 
- SDK 없이 문서만 있으면 "이렇게 하세요"만 있고 실제 도움이 안 됨
- SDK가 있어야 "이 함수 호출하세요"로 간단해짐

### 4. 타겟 사용자

```
C) 우리 3개 팀만 (현재)
   → B) 사내 개발자 (3개월 후)
   → A) 외부 개발자 (6개월 후)
```

**동의합니다.** 우리가 먼저 써보고 검증해야 합니다.

### 5. 외부 확장 현실성

**현재는 비현실적입니다.** 이유:
- 문서 부족
- 예제 부족
- 지원 체계 없음

**하지만 6개월 후에는 가능합니다.** 조건:
- SDK 안정화
- 문서/예제 충분
- 사내 검증 완료

---

## 🔴 Full SDK vs Extensions 전략에 대한 의견

### 오케스트레이터 팀 제안

```
Full SDK (전체 래핑) → Extensions Only (거버넌스만)
```

### Agent Team 의견: **하이브리드 제안**

```
Core SDK (Thin) + Extensions (Governance)
```

| 레이어 | 내용 | 유지보수 |
|--------|------|----------|
| **Core SDK (Thin)** | A2A 응답 빌더, MCPHub 클라이언트 | 우리 (최소) |
| **Extensions** | K-Auth, Agent Card, 로깅 | 우리 |
| **프로토콜 직접 사용** | A2A JSON-RPC, MCP 공식 SDK | Anthropic/개발자 |

### 이유

1. **A2A JSON-RPC 직접 구현은 비효율**
   - 메서드 호환성 (SendMessage, message/send 등) 매번 구현?
   - Part 형식 변환 (표준/레거시) 매번 구현?
   - → **응답 빌더**라도 있어야 함

2. **MCPHub 연동은 래퍼 필요**
   - MCP 공식 SDK는 Stateful 세션 기대
   - MCPHub는 Stateless
   - → **우리만의 HTTP 클라이언트** 필요

3. **하지만 전체 래핑은 과함**
   - MCP 프로토콜 전체 래핑 X
   - A2A 프로토콜 전체 래핑 X
   - → **최소한의 헬퍼만** 제공

### 제안 구조

```python
# 개발자가 작성하는 코드
from k_jarvis import A2AServer, A2AResponse, MCPClient
from k_jarvis.config import BaseAgentSettings
from k_jarvis.extensions import KAuthHelper, AgentCardGenerator

# A2A 서버 (Thin SDK)
server = A2AServer(name="My Agent", port=5010)

# MCP 클라이언트 (Thin SDK - Stateless HTTP)
client = MCPClient(base_url=settings.mcp_hub_url)

# Extensions (거버넌스)
kauth = KAuthHelper()
card = AgentCardGenerator.create(...)

@server.message_handler
async def handle(text, params):
    # 비즈니스 로직
    result = await client.call_tool("search", {"query": text})
    return A2AResponse.text(result)  # 응답 빌더
```

---

## 📊 유지보수 부담 분석

### 우리가 만든 SDK 범위

| 모듈 | 외부 스펙 의존도 | 변경 가능성 |
|------|-----------------|-------------|
| `A2AResponse` (응답 빌더) | **낮음** - Part 형식만 | 낮음 |
| `A2AServer` (Flask 서버) | **낮음** - JSON-RPC 기본 | 낮음 |
| `MCPClient` (HTTP 클라이언트) | **낮음** - JSON-RPC 기본 | 낮음 |
| `BaseAgentSettings` | **없음** - 우리 거버넌스 | 우리 결정 |
| `ErrorCode` | **낮음** - 표준 코드 | 낮음 |

### 결론

**우리가 만든 SDK는 프로토콜 전체를 래핑하지 않습니다.**

- MCP 프로토콜 자체는 래핑 X (공식 SDK 사용 권장)
- A2A 프로토콜 자체는 래핑 X (JSON-RPC 직접 사용 가능)
- **거버넌스 헬퍼만** 제공

→ **외부 스펙 변경 영향 최소화**

---

## ✅ 결론 및 제안

### 1. SDK 개발 방향

```
Extensions Only가 아니라, Thin Core SDK + Extensions 제안
```

- Thin Core: A2AResponse, MCPClient, BaseAgentSettings
- Extensions: KAuthHelper, AgentCardGenerator, StandardLogger

### 2. 우선순위

```
Phase 1 (현재):
├─ Thin Core SDK (우리가 이미 만듦)
├─ 기본 문서 (README)
└─ 예제 (Sample Agent)

Phase 2 (1개월):
├─ Extensions 추가
├─ 상세 문서
└─ 사내 개발자 테스트

Phase 3 (3개월):
├─ CLI 도구
├─ 프로젝트 템플릿
└─ 외부 확장 검토
```

### 3. 타겟

**현재는 우리 3개 팀만.** 내부 검증 후 확장.

---

## 📎 첨부: SDK 적용 코드 비교

### 기존 코드 (364줄)

```python
# 수많은 보일러플레이트...
# A2A 프로토콜 직접 구현
# 메서드 호환성 처리
# 응답 포맷팅
# ...
```

### SDK 적용 후 (138줄)

```python
from k_jarvis import A2AServer, A2AResponse, MCPClient
from k_jarvis.config import BaseAgentSettings

settings = SampleAgentSettings()
client = MCPClient(base_url=settings.mcp_hub_url)
server = A2AServer(name="Sample Agent", port=settings.agent_port)

@server.message_handler
async def handle(text, params):
    tools = await client.list_tools()
    return A2AResponse.text(f"도구 {len(tools)}개 사용 가능")

server.run()
```

**62% 코드 감소!**

---

**Agent Team**  
Slack: #agent-dev

