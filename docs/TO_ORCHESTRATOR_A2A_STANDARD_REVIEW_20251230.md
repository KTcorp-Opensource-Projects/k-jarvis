# A2A 표준 스펙 준수 검토 요청

**작성일**: 2025-12-30  
**작성팀**: Agent Team  
**대상**: Orchestrator Team  
**상태**: 📋 검토 요청

---

## 📋 배경

Agent Team에서 Google A2A 공식 스펙을 분석한 결과, 현재 K-Jarvis 구현과 표준 스펙 간에 일부 차이점을 발견했습니다.

**표준 준수의 중요성**:
- 외부 개발자가 K-Jarvis 플랫폼에 에이전트를 쉽게 연동
- 다른 A2A 호환 시스템과의 상호운용성 확보
- 생태계 확장성

---

## 🔍 분석 결과

### A2A JSON-RPC 표준 스펙 (Google 공식)

**메서드 이름**: PascalCase

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "SendMessage",  // ← PascalCase
  "params": {
    "message": {
      "role": "user",
      "parts": [{ "text": "Hello" }]
    }
  }
}
```

**표준 응답 구조**:

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "task": { /* Task object */ }
    // 또는
    "message": { /* Message object */ }
  }
}
```

### 현재 K-Jarvis 구현

**메서드 이름**: lowercase/slash

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "message/send",  // ← lowercase/slash
  "params": {
    "message": {
      "role": "user",
      "parts": [{ "type": "text", "text": "Hello" }]
    }
  }
}
```

**현재 응답 구조**:

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "artifacts": [{ /* ... */ }]  // ← 비표준
  }
}
```

---

## 📊 차이점 요약

| 항목 | A2A 표준 | K-Jarvis 현재 | 영향도 |
|------|---------|--------------|--------|
| **메서드 이름** | `SendMessage` | `message/send` | 🔴 높음 |
| **응답 구조** | `result.task` / `result.message` | `result.artifacts` | 🔴 높음 |
| **Part 구조** | `{ "text": "..." }` | `{ "type": "text", "text": "..." }` | 🟡 중간 |
| **엔드포인트** | `/rpc` 또는 `/` | `/a2a`, `/tasks/send` | 🟢 낮음 |

---

## 🤔 검토 요청 사항

### 질문 1: 메서드 이름 변경 가능 여부

오케스트레이터에서 A2A 표준 메서드 이름을 사용할 수 있나요?

**변경 전**:
```python
method = "message/send"
```

**변경 후 (표준)**:
```python
method = "SendMessage"
```

### 질문 2: 응답 구조 변경 가능 여부

표준 응답 구조로 변경 가능한지 검토 부탁드립니다:

**변경 전**:
```json
{
  "result": {
    "artifacts": [{ "parts": [...] }]
  }
}
```

**변경 후 (표준)**:
```json
{
  "result": {
    "message": {
      "role": "agent",
      "parts": [{ "text": "..." }]
    }
  }
}
```

### 질문 3: Part 구조

표준에서는 Part가 직접 `text`, `file`, `data` 키를 가집니다:

**표준**:
```json
{ "text": "Hello" }
{ "file": { "name": "...", "mediaType": "..." } }
{ "data": { "key": "value" } }
```

**현재**:
```json
{ "type": "text", "text": "Hello" }
```

호환성을 위해 둘 다 지원하는 것이 좋을까요?

---

## 💡 Agent 팀 제안

### 옵션 A: 표준 완전 준수 (권장)

1. 메서드 이름: `SendMessage`, `GetTask` 등 PascalCase
2. 응답 구조: `result.task` 또는 `result.message`
3. Part 구조: 표준 형식 사용

**장점**:
- 외부 A2A 호환 시스템과 상호운용 가능
- 외부 개발자 진입 장벽 낮춤
- 표준 A2A SDK 사용 가능

### 옵션 B: 하위 호환성 유지

1. 표준 메서드와 기존 메서드 모두 지원
2. 응답 구조 점진적 마이그레이션
3. Part 구조 양방향 호환

**장점**:
- 기존 시스템 영향 최소화
- 점진적 전환 가능

### 옵션 C: 현재 유지

1. K-Jarvis 전용 스펙으로 유지
2. 외부 연동 시 어댑터 레이어 제공

**장점**:
- 변경 비용 없음

**단점**:
- 외부 개발자 진입 장벽
- 표준 SDK 사용 불가

---

## 📅 제안 일정

표준 준수로 결정 시:

| 단계 | 내용 | 예상 기간 |
|------|------|----------|
| 1 | 스펙 합의 | 1주 |
| 2 | 오케스트레이터 수정 | 1주 |
| 3 | Agent 수정 | 1주 |
| 4 | 통합 테스트 | 1주 |

---

## 📞 연락처

**Agent Team**  
Slack: #agent-dev

---

**의견 부탁드립니다!** 🙏

---

## 📎 참고 자료

- [Google A2A Specification](https://github.com/google/A2A/blob/main/docs/specification.md)
- A2A JSON-RPC Binding: Section 9
- A2A Core Methods: Section 9.4

